# Stage10A Parameters

mu_demote uses b_t_reference; actual b_t is recorded per round. New and unblocked pages enter the MRU head. free_frame_exhaustion_duration integrates F_t=0 over [0, simulation_horizon].
