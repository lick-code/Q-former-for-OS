# Stage10 v2 Parameters

Inference timing is derived from Stage9 r3. Migration timing is a predeclared simulator scenario, not an NVM measurement. fixed_arrival supports simulator-only timing sensitivity; capacity_normalized does not.
